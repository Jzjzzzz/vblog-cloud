package team.blogserver.common.mapper;

import org.springframework.stereotype.Repository;
import team.blogserver.common.model.domain.WebConfig;

@Repository
public interface WebConfigMapper extends Mapper<WebConfig> {
}
